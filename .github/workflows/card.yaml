name: Update Profile Card
on:
  schedule:
    - cron: '0 0 * * *' 
  workflow_dispatch:

jobs:
  card:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: hugefiver/logit@master
        with:
          username: ${{ github.actor }}
          token: ${{secrets.GH_PAT}}
          command: multi
          periods: week,month,year
          include-contributed: 'true'
          include-private: 'true'
          short: true
          output: multi-card.svg
          exclude-lang: java,bash,shell,markdown,javascript,css,sass,html

      - uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: 'chore: update profile card'
          file_pattern: 'multi-card.svg'
